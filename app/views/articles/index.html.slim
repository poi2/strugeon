h1 Listing articles

table
  thead
    tr
      th Title
      th Description
      th Target amount
      th Closed at
      th Innovators
      th
      th
      th

  tbody
    - @articles.each do |article|
      tr
        td = article.title
        td = index_description article
        td = number_to_currency article.target_amount
        td = article.closed_at
        td = self_innovator_list article
        td = link_to 'Show', article
        td = link_to 'Edit', edit_article_path(article)
        td = link_to 'Destroy', article, data: {:confirm => 'Are you sure?'}, :method => :delete

br

= link_to 'New Article', new_article_path
